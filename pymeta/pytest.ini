# pytest defaults (if not in pyproject)
# Most pytest configuration is now in pyproject.toml
# This file is optional and can be used for pytest-specific settings

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*